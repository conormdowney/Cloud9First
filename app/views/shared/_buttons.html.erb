<div class='article-actions'>
    <%= link_to "Edit this article", edit_article_path(obj), class: "btn btn-xs btn-primary" %>
    <%= link_to "Delete this article", article_path(obj), method: :delete, data: {confirm: "Are you sure?"}, 
                    class: "btn btn-xs btn-danger" %>
</div>